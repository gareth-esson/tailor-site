---
/**
 * Service page [C5] — /services/drop-days
 * RSE drop day delivery. Tailor layer, v3.
 * Lean content — fewer feature cards than full delivery pages.
 */
import BaseLayout from '../../layouts/BaseLayout.astro';
import CtaServiceEnquiry from '../../components/CtaServiceEnquiry.astro';
import ServiceTopicsStrip from '../../components/ServiceTopicsStrip.astro';
import { getBlogPosts } from '../../lib/content';

// --- Data ----------------------------------------------------------
const slug = 'drop-days';
const title = 'RSE drop days';
const serviceParam = 'Drop day delivery';
const serviceName = 'drop days';

const heroSubtitle =
  'A full day of expert-led RSE delivery across multiple year groups, designed around your cohort and covered in one go. Maximum impact, minimum disruption to the rest of the timetable.';

const heroImage: string | null = '/images/services/tailor-education-rse-year-group-session-school-hall.webp';
const heroImageAlt = 'A whole year group gathered in the school hall for a Tailor drop day RSE session';

const descriptionLead =
  'Some schools prefer to concentrate their RSE delivery into a single, high-quality day rather than spread it thinly across the year. A drop day brings a specialist educator into your school for a full day of sessions, rotating through year groups or forms so the whole cohort is covered at once.';

const descriptionBody = [
  'Drop days are particularly useful when you want to guarantee every student has heard the same content, delivered by the same expert voice, within a defined window. We work with you on topic selection, session order, and how the day fits around your existing timetable.',
];

const whatSchoolsGet: string | null = null;

const featureCards = [
  { title: 'Full-day expert delivery', description: 'A specialist educator on-site for the full school day, rotating through sessions with your cohorts.' },
  { title: 'Multi-cohort coverage', description: 'Reach an entire year group — or multiple year groups — in a single day. Every student hears the same content.' },
  { title: 'Topic selection with your PSHE lead', description: 'We agree topics, session order, and cohort grouping in advance so the day fits your curriculum and context.' },
  { title: 'Post-day summary and follow-up', description: 'Written debrief for staff including talking points, disclosure watch-list, and recommended follow-up activities.' },
];

// Drop days has no dedicated process block in v3 — the flow is self-explanatory
const processSteps = null;

const testimonialQuote =
  'One day, every student in the year group covered — and delivered better than anything we could have put together ourselves. It let us protect the rest of the curriculum while still doing RSE properly.';
const testimonialSource = 'Assistant Head, secondary school';

const featuredTopicSlugs = [
  'consent',
  'healthy-relationships',
  'online-safety',
  'masculinity-and-misogyny',
  'mental-health-and-wellbeing',
  'sexting-and-sharing-nudes',
];

// --- Content --------------------------------------------------------
const allPosts = await getBlogPosts();
const relatedPosts = allPosts.filter((p) => p.serviceLink === 'drop-days').slice(0, 3);

const metaTitle = `${title} — RSE for Schools | Tailor Education`;
const metaDescription = 'A full day of expert-led RSE delivery across multiple year groups. Maximum impact, minimum disruption.';
---

<BaseLayout
  title={metaTitle}
  description={metaDescription}
  canonicalPath={`/services/${slug}/`}
  ogImage={heroImage ?? undefined}
  jsonLd={{
    '@context': 'https://schema.org',
    '@type': 'Service',
    name: title,
    description: metaDescription,
    provider: { '@type': 'Organization', name: 'Tailor Education', url: 'https://tailoreducation.org.uk' },
  }}
>
  <article class="service-page" data-pagefind-body>

    <header class="service-hero">
      <div class="mx-auto w-full max-w-[var(--container-max-shell)] px-[var(--space-global-gutter)] py-[var(--space-struct-y-base)]">
        <div class="service-hero__inner">
          <div class="service-hero__text">
            <span class="service-hero__eyebrow">Services</span>
            <h1 class="service-hero__title">{title}</h1>
            <p class="service-hero__subtitle">{heroSubtitle}</p>
            <a
              href={`/contact?service=${encodeURIComponent(serviceParam)}`}
              class="btn btn--std btn--primary has-icon-hover"
              data-analytics-event="cta_click"
              data-analytics-cta-type="hero_enquire"
              data-analytics-service-type={serviceParam}
            >Enquire now</a>
          </div>
          <div class="service-hero__image">
            {heroImage ? (
              <img src={heroImage} alt={heroImageAlt} loading="eager" />
            ) : (
              <div class="service-hero__image-placeholder">Hero photography required — source before launch</div>
            )}
          </div>
        </div>
      </div>
    </header>

    <section class="service-description">
      <div class="mx-auto w-full max-w-[var(--container-max-prose)] px-[var(--space-global-gutter)] py-[var(--space-struct-y-base)]">
        <p class="service-description__lead">{descriptionLead}</p>
        {descriptionBody.map((para) => (<p>{para}</p>))}
        {whatSchoolsGet && (
          <>
            <h2 class="service-description__sub-title">What schools get</h2>
            <p>{whatSchoolsGet}</p>
          </>
        )}
      </div>
    </section>

    <section class="service-features">
      <div class="mx-auto w-full max-w-[var(--container-max-shell)] px-[var(--space-global-gutter)] py-[var(--space-struct-y-base)]">
        <h2 class="service-section-title">What's included</h2>
        <div class="service-features__grid">
          {featureCards.map((card) => (
            <div class="feature-card">
              <h3 class="feature-card__title">{card.title}</h3>
              <p class="feature-card__text">{card.description}</p>
            </div>
          ))}
        </div>
      </div>
    </section>

    {processSteps && (
      <section class="service-process">
        <div class="mx-auto w-full max-w-[var(--container-max-shell)] px-[var(--space-global-gutter)] py-[var(--space-struct-y-base)]">
          <h2 class="service-section-title">How it works</h2>
          <div class="service-process__grid">
            {processSteps.map((step) => (
              <div class="process-step">
                <div class="process-step__number">{step.number}</div>
                <h3 class="process-step__title">{step.title}</h3>
                <p class="process-step__text">{step.description}</p>
              </div>
            ))}
          </div>
        </div>
      </section>
    )}

    <section class="service-proof-band">
      <div class="mx-auto w-full max-w-[var(--container-max-prose)] px-[var(--space-global-gutter)] py-[var(--space-struct-y-base)]">
        <div class="service-proof-band__inner">
          {testimonialQuote && (
            <>
              <blockquote>
                <p class="service-proof-band__quote">"{testimonialQuote}"</p>
              </blockquote>
              <span class="service-proof-band__source">— {testimonialSource}</span>
            </>
          )}
          <div class="service-proof-band__cta-wrap">
            <a
              href={`/contact?service=${encodeURIComponent(serviceParam)}`}
              class="btn btn--std btn--white-primary has-icon-hover"
              data-analytics-event="cta_click"
              data-analytics-cta-type="proof_band_enquire"
              data-analytics-service-type={serviceParam}
            >Enquire about {serviceName}</a>
            <a href="/testimonials" class="service-proof-band__testimonials-link">Read more testimonials →</a>
          </div>
        </div>
      </div>
    </section>

    {relatedPosts.length > 0 && (
      <section class="service-blog">
        <div class="mx-auto w-full max-w-[var(--container-max-shell)] px-[var(--space-global-gutter)] py-[var(--space-struct-y-base)]">
          <h2 class="service-section-title">Related articles</h2>
          <div class="service-blog__grid">
            {relatedPosts.map((bp) => (
              <a href={`/blog/${bp.slug}`} class="card card--lift service-blog-card">
                {bp.featuredImage && (
                  <div class="service-blog-card__img">
                    <img src={bp.featuredImage} alt="" loading="lazy" />
                  </div>
                )}
                <div class="card__body">
                  {bp.targetAudience && <div class="card__eyebrow">{bp.targetAudience}</div>}
                  <h3 class="card__title">{bp.title}</h3>
                  {bp.author && <div class="service-blog-card__author">By {bp.author}</div>}
                </div>
              </a>
            ))}
          </div>
        </div>
      </section>
    )}

    <ServiceTopicsStrip featuredSlugs={featuredTopicSlugs} />

    <CtaServiceEnquiry serviceName={serviceName} serviceParam={serviceParam} variant="alt" />

  </article>
</BaseLayout>
