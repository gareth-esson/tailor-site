---
/**
 * Redirect: /services/rse-delivery → /services
 * The v3 spec removes the hub page — teachers arriving at /services
 * pick their delivery variant directly.
 */
return Astro.redirect('/services', 301);
---
