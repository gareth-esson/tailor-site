---
/**
 * Redirect: /training → /services/rse-training
 * Old B4 route preserved for backwards compatibility.
 * RSE Training now lives at /services/rse-training as C5.
 */
return Astro.redirect('/services/rse-training', 301);
---
