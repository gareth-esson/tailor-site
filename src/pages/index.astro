---
/**
 * Homepage [B1] — /
 * Service-first. Single Tailor layer; OtA module dropped in favour of a
 * book signpost panel that promotes the book and links across to the OtA
 * questions library.
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import HeroImage from '../components/HeroImage.astro';
import { heroPreload } from '../lib/hero-image';
import { getLandingPages, getTestimonials } from '../lib/content';
import { getTopicIllustrationPath } from '../lib/topic-illustrations';

const allTestimonials = await getTestimonials();
const homeTestimonial = allTestimonials.find((t) => t.name === 'Sara Stafford') ?? allTestimonials[0] ?? null;

// §7 Topic carousel data
const landingPages = await getLandingPages();
const categoryTokenMap: Record<string, string> = {
  'Relationships': 'relationships',
  'Sex & Sexual Health': 'sexual-health',
  'Puberty & The Body': 'puberty',
  'Identity & Diversity': 'identity',
  'Online Safety & Media': 'online-safety',
  'Safety & Safeguarding': 'safety',
  'Health & Wellbeing': 'wellbeing',
};

// Hero photograph — conditionally rendered
const heroPhoto = '/images/services/tailor-education-secondary-rse-group-activity.webp' as string | null;

// §3 delivery photograph — conditionally rendered
const deliveryPhoto = '/images/services/tailor-education-primary-rse-discussion-circle.webp' as string | null;

// §4 training photograph — conditionally rendered
const trainingPhoto = '/images/services/tailor-education-teachers-reviewing-rse-resources.webp' as string | null;

---

<BaseLayout
  title="Tailor Education — RSE Training & Delivery for UK Schools"
  description="Expert RSE for UK primary and secondary schools. In-school teaching, CPD, and the Okay to Ask book."
  canonicalPath="/"
  {...(heroPhoto ? heroPreload(heroPhoto, { media: '(min-width: 768px)' }) : {})}
>
  <!-- ═══ §1 TEACHER HERO ═══ -->
  <section class="hero-teacher" aria-labelledby="hero-heading">
    <div class="hero-teacher__inner">
      <div class="hero-teacher__text">
        <p class="preheader hero-teacher__eyebrow">Tailor</p>
        <h1 id="hero-heading" class="hero-teacher__title">RSE that helps young people enjoy healthier relationships.</h1>
        <p class="hero-teacher__subtitle">
          KS1 to KS5 lessons, schemes of work, and CPD designed and taught by a practising specialist.
        </p>
        <div class="hero-teacher__actions">
          <a href="/contact" class="btn btn--std btn--primary has-icon-hover w-full sm:w-auto">
            Talk to us about RSE
          </a>
          <a href="/services" class="btn btn--std btn--tint has-icon-hover w-full sm:w-auto">
            See services
          </a>
        </div>
      </div>
      {heroPhoto && (
        <div class="hero-photo-wrap hidden md:block">
          <HeroImage
            src={heroPhoto}
            alt="Secondary students in a Tailor RSE group session"
            class="hero-photo"
            width={1600}
            height={900}
            priority
          />
        </div>
      )}
    </div>
  </section>

  <!-- ═══ §1.5 ABOVE-FOLD TRUST LOGOS ═══ -->
  <section class="home-trustlogos surface--alt" aria-labelledby="trustlogos-heading">
    <div class="home-trustlogos__inner">
      <h2 id="trustlogos-heading" class="home-trustlogos__heading">Working with</h2>
      <ul class="home-trustlogos__logos" role="list">
        <li class="home-trustlogos__logo-item">
          {/* British Red Cross emblem — protected under the Geneva
              Conventions Act 1957; renders in its authorised red. */}
          <img src="/assets/logos/british-red-cross.svg" alt="The British Red Cross" class="home-trustlogos__logo home-trustlogos__logo--colour" loading="lazy" decoding="async" />
        </li>
        <li class="home-trustlogos__logo-item">
          <img src="/assets/logos/somerset-house.svg" alt="Somerset House" class="home-trustlogos__logo home-trustlogos__logo--square" loading="lazy" decoding="async" />
        </li>
        <li class="home-trustlogos__logo-item">
          <img src="/assets/logos/kings-college-london.svg" alt="King's College London" class="home-trustlogos__logo" loading="lazy" decoding="async" />
        </li>
        <li class="home-trustlogos__logo-item">
          <img src="/assets/logos/british-museum.svg" alt="The British Museum" class="home-trustlogos__logo" loading="lazy" decoding="async" />
        </li>
        <li class="home-trustlogos__logo-item">
          <img src="/assets/logos/rada.svg" alt="Royal Academy of Dramatic Art" class="home-trustlogos__logo" loading="lazy" decoding="async" />
        </li>
        <li class="home-trustlogos__logo-item">
          <img src="/assets/logos/tate-modern.svg" alt="Tate Modern" class="home-trustlogos__logo" loading="lazy" decoding="async" />
        </li>
      </ul>
    </div>
  </section>

  <!-- ═══ §2 IN-SCHOOL DELIVERY (was §3) ═══ -->
  <section class="home-delivery" aria-labelledby="delivery-heading">
    <div class="home-delivery__inner split-layout split-layout--image-left">
      <div class="home-delivery__text split-layout__text">
        <h2 id="delivery-heading" class="home-delivery__title">RSE, taught directly in your school</h2>
        <p class="home-delivery__desc">
          Most of what Tailor does is direct teaching. A school that wants an external specialist on RSE books a Tailor educator, and one of us takes the lessons. We work across primary, secondary, SEND and alternative provision.
        </p>
        <p class="home-delivery__desc">
          Every lesson is written for your class. We'll ask your PSHE lead what we need to know going in, a recent safeguarding issue, a topic that needs more time, a pupil to plan around, and shape the session around what you tell us. After we've taught it, your staff get a written debrief and notes they can use the next week.
        </p>
        <a href="/services" class="btn btn--std btn--primary has-icon-hover w-full sm:w-auto">
          Explore delivery options
        </a>
      </div>
      {deliveryPhoto && (
        <figure class="home-delivery__photo split-layout__media hidden md:block">
          <img
            src={deliveryPhoto}
            alt="A Tailor educator leading a primary RSE discussion circle"
            loading="lazy"
          />
        </figure>
      )}
    </div>
  </section>

  <!-- ═══ §3 TRAINING & SUPPORT (was §4) ═══ -->
  <section class="home-training surface--alt" aria-labelledby="training-heading">
    <div class="home-training__inner split-layout">
      <div class="home-training__text split-layout__text">
        <h2 id="training-heading" class="home-training__title">Help your team teach this with confidence</h2>
        <p class="home-training__intro">
          RSE is hard to teach well if you haven't been trained in it. Most teaching teams haven't. That gap shows up the moment a year 9 puts their hand up with something you didn't prep for, or when the parent email arrives the day after a contentious topic.
        </p>
        <p class="home-training__detail">
          Our CPD is built around what we use ourselves in classrooms, the subject knowledge, the strategies for the harder conversations, the read of the statutory guidance that lets a head of PSHE plan a year of RSE without panic-checking the document every term. Sessions run in your school or online, around the school calendar.
        </p>
        <div class="home-training__actions">
          <a href="/services/rse-training" class="btn btn--std btn--primary has-icon-hover w-full sm:w-auto">
            Explore training
          </a>
          <a href="/contact" class="btn btn--std btn--tint has-icon-hover w-full sm:w-auto">
            Talk to us about support
          </a>
        </div>
      </div>
      {trainingPhoto && (
        <figure class="home-training__photo split-layout__media hidden md:block">
          <img
            src={trainingPhoto}
            alt="Teachers reviewing RSE resources together during CPD"
            loading="lazy"
          />
        </figure>
      )}
    </div>
  </section>

  <!-- ═══ §5 TRUST SIGNALS ═══ -->
  <section class="home-trust surface--alt" aria-labelledby="trust-heading">
    <div class="home-trust__inner">
      <div class="home-trust__badges-wrap">
        <h2 id="trust-heading" class="home-trust__title">Aligned with the guidance schools are held to</h2>
        <div class="home-trust__badges">
          <div class="trust-badge">
            <span>DfE Statutory RSE Guidance</span>
          </div>
          <div class="trust-badge">
            <span>PSHE Association Programme of Study</span>
          </div>
          <div class="trust-badge">
            <span>UNESCO International Technical Guidance</span>
          </div>
          <div class="trust-badge">
            <span>WHO Standards for Sexuality Education</span>
          </div>
        </div>
      </div>

      {homeTestimonial && (
      <div class="home-trust__quote-wrap">
        <blockquote class="home-trust__quote">
          <p class="home-trust__quote-text">
            "{homeTestimonial.quote}"
          </p>
          <div class="home-trust__attribution">
            <cite class="home-trust__cite">
              {[homeTestimonial.name, homeTestimonial.role, homeTestimonial.organisation].filter(Boolean).join(', ')}
            </cite>
          </div>
        </blockquote>
        <div class="home-trust__action">
          <a href="/our-approach" class="btn btn--std btn--tint has-icon-hover w-full sm:w-auto">See our approach</a>
        </div>
      </div>
      )}
    </div>
  </section>

  <!-- ═══ §5.5 SELECTED PROJECTS ═══ -->
  <section class="home-projects" aria-labelledby="projects-heading">
    <div class="home-projects__inner">
      <h2 id="projects-heading" class="home-projects__title">We also work outside schools</h2>
      <p class="home-projects__body">
        We work with museums, universities, drama schools and the third sector on RSE projects for adults and young people in different settings. The sessions and workshops we've delivered for these partners feed directly back into what we teach in classrooms.
      </p>

      <ul class="home-projects__logos" role="list">
        <li class="home-projects__logo-item">
          {/* British Red Cross — emblem is protected under the Geneva
              Conventions Act 1957; must render in its authorised red.
              No monochrome filter is applied. */}
          <img src="/assets/logos/british-red-cross.svg" alt="The British Red Cross" class="home-projects__logo home-projects__logo--colour" loading="lazy" />
        </li>
        <li class="home-projects__logo-item">
          {/* Square logos under-read at equal height against wide logos;
              taller modifier balances the row's optical weight. */}
          <img src="/assets/logos/somerset-house.svg" alt="Somerset House" class="home-projects__logo home-projects__logo--square" loading="lazy" />
        </li>
        <li class="home-projects__logo-item">
          <img src="/assets/logos/kings-college-london.svg" alt="King's College London" class="home-projects__logo" loading="lazy" />
        </li>
        <li class="home-projects__logo-item">
          <img src="/assets/logos/british-museum.svg" alt="The British Museum" class="home-projects__logo" loading="lazy" />
        </li>
        <li class="home-projects__logo-item">
          <img src="/assets/logos/rada.svg" alt="Royal Academy of Dramatic Art" class="home-projects__logo" loading="lazy" />
        </li>
        <li class="home-projects__logo-item">
          <img src="/assets/logos/tate-modern.svg" alt="Tate Modern" class="home-projects__logo" loading="lazy" />
        </li>
      </ul>

      <div class="home-projects__action">
        <a href="/blog/category/our-work" class="btn btn--std btn--tint has-icon-hover w-full sm:w-auto">
          See our projects
        </a>
      </div>
    </div>
  </section>

  <!-- ═══ §6 OKAY TO ASK BOOK ═══ -->
  <section class="home-book" aria-labelledby="book-heading">
    <div class="home-book__inner split-layout split-layout--image-left">
      <div class="home-book__text split-layout__text">
        <h2 id="book-heading" class="home-book__title">The questions pupils want answered, in book form</h2>
        <p class="home-book__desc">
          Okay to Ask started as a stack of post-it questions handed in anonymously at the end of lessons. The book collects them with proper answers from a practising RSE specialist, written for pupils, useful for the adults who teach them.
        </p>
        <div class="home-book__actions">
          <a href="/book" class="btn btn--std btn--primary has-icon-hover w-full sm:w-auto">
            Pre-order the book
          </a>
          <a href="/questions/" class="btn btn--std btn--tint has-icon-hover w-full sm:w-auto">
            Read questions on Okay to Ask
          </a>
        </div>
      </div>
      <div class="home-book__cover-wrap">
        <img
          src="/images/ota-book-product-front.webp"
          alt="The Okay to Ask book, front cover with a handwritten post-it question and the Okay to Ask wordmark"
          class="home-book__cover"
          loading="lazy"
        />
      </div>
    </div>
  </section>

  <!-- ═══ §7 TOPICS CAROUSEL ═══ -->
  <section class="home-topics" aria-labelledby="topics-heading">
    <div class="home-topics__inner">
      <div class="home-topics__header">
        <h2 id="topics-heading" class="home-topics__title">Topics we cover</h2>
        <p class="home-topics__intro">
          Expert-written RSE content across {landingPages.length} curriculum-aligned topics.
        </p>
      </div>

      <div class="home-topics__track">
        {landingPages.map((lp) => {
          const token = categoryTokenMap[lp.category || ''] || 'relationships';
          const illustration = getTopicIllustrationPath(lp.slug);
          return (
            <a href={`/topics/${lp.slug}`} class="home-topics__card" style={`--cat-color: var(--cat-${token})`}>
              <div class="home-topics__card-illus">
                {illustration && <img src={illustration} alt="" loading="lazy" />}
              </div>
              <span class="home-topics__card-category">{lp.category}</span>
              <span class="home-topics__card-title">{lp.title}</span>
            </a>
          );
        })}
        <a href="/topics/" class="home-topics__card home-topics__card--all">
          <span class="home-topics__card-title">View all topics</span>
          <span class="link-action">Browse the full library</span>
        </a>
      </div>

      <div class="home-topics__nav" aria-hidden="true">
        <button class="home-topics__arrow home-topics__arrow--prev" type="button" aria-label="Scroll left" disabled>
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none"><path d="M12.5 15L7.5 10L12.5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </button>
        <button class="home-topics__arrow home-topics__arrow--next" type="button" aria-label="Scroll right">
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none"><path d="M7.5 15L12.5 10L7.5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </button>
      </div>
    </div>
  </section>

  <!-- ═══ §8 CLOSING CTA ═══ -->
  <section class="home-cta surface--dark" aria-labelledby="cta-heading">
    <div class="home-cta__inner">
      <h2 id="cta-heading" class="home-cta__title">Want to talk about RSE at your school?</h2>
      <p class="home-cta__subtitle">
        Tell us where you're starting from, and we'll work out what Tailor can do.
      </p>
      <div class="home-cta__actions">
        <a href="/contact" class="btn btn--std btn--primary has-icon-hover w-full sm:w-auto">
          Get in touch
        </a>
        <a href="/services" class="btn btn--std btn--tint has-icon-hover w-full sm:w-auto">
          Explore services
        </a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* ========================================
     Homepage [B1], Layout Spec v2.0
     Teacher-first. 8 sections. 1 layer switch.
     ======================================== */

  /* --- §1 Teacher hero ---
     Full-bleed split at lg+: text cell shell-aligned on left, image
     cell bleeds to viewport's right edge and fills the section height
     (image cropped via object-fit: cover). Below lg the image is
     hidden (existing .hidden.lg:block) and the text fills the width. */
  .hero-teacher {
    background-color: var(--bg-page);
  }

  .hero-teacher__inner {
    width: 100%;
    display: grid;
    grid-template-columns: 1fr;
  }

  .hero-teacher__text {
    max-width: var(--container-max-hero-text);
    padding: var(--space-struct-y-hero) var(--space-global-gutter);
    padding-left: max(
      var(--space-global-gutter),
      calc((100vw - var(--container-max-shell)) / 2)
    );
  }

  .hero-teacher__eyebrow {
    color: var(--text-heading-muted);
    margin: 0 0 var(--space-global-sm);
  }

  .hero-teacher__title {
    font-family: var(--font-tailor-heading-stack);
    font-size: var(--text-display-size-h1);
    font-weight: var(--heading-weight-h1);
    line-height: var(--lh-display);
    color: var(--text-heading);
    margin: 0 0 var(--space-global-md);
  }

  .hero-teacher__subtitle {
    font-family: var(--font-tailor-body-stack);
    font-size: var(--text-util-hero-subhead-size);
    font-weight: var(--font-weight-regular);
    line-height: var(--lh-heading-sub);
    color: var(--text-body-muted);
    margin: 0 0 var(--space-global-lg);
  }

  .hero-teacher__actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-global-sm);
  }

  .hero-photo-wrap {
    /* Grid cell; image fills it. */
    min-height: 100%;
  }

  /* <picture> and <img> are rendered by the HeroImage child component,
     so they fall outside this page's scoped CSS. :global() lets these
     rules reach them. */
  .hero-photo-wrap :global(picture) {
    display: block;
    width: 100%;
    height: 100%;
  }

  .hero-photo-wrap :global(.hero-photo) {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  /* --- §1.5 Above-fold trust logos --- */
  .home-trustlogos {
    padding: var(--space-global-md) var(--space-global-gutter);
    border-bottom: var(--border-width-xs) solid var(--border-subtle);
  }

  .home-trustlogos__inner {
    max-width: var(--container-max-shell);
    margin: 0 auto;
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-global-sm);
  }

  .home-trustlogos__heading {
    font-family: var(--font-tailor-body-stack);
    font-size: var(--text-card-size-body);
    font-weight: var(--font-weight-semibold);
    text-transform: uppercase;
    letter-spacing: var(--section-eyebrow-ls);
    color: var(--text-body-muted);
    margin: 0;
  }

  .home-trustlogos__logos {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    gap: var(--space-global-lg);
  }

  .home-trustlogos__logo-item {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .home-trustlogos__logo {
    display: block;
    height: 2rem;
    width: auto;
    max-width: 8rem;
    object-fit: contain;
    filter: grayscale(1) opacity(0.65);
  }

  /* Full-colour override — Red Cross emblem under mark-protection rules. */
  .home-trustlogos__logo--colour {
    filter: none;
  }

  /* Square-logo bump — matches optical weight against wide marks. */
  .home-trustlogos__logo--square {
    height: 3.5rem;
  }

  @media (max-width: 767px) {
    .home-trustlogos__logos {
      gap: var(--space-global-md);
    }

    .home-trustlogos__logo {
      height: 1.5rem;
    }

    .home-trustlogos__logo--square {
      height: 2.75rem;
    }
  }

  /* --- §3 Delivery --- */
  .home-delivery {
    padding: var(--space-struct-y-base) var(--space-global-gutter);
  }

  .home-delivery__inner {
    max-width: var(--container-max-shell);
    margin: 0 auto;
    width: 100%;
  }

  .home-delivery__text {
    max-width: var(--container-max-prose);
  }

  .home-delivery__photo {
    box-shadow: var(--shadow-md);
  }

  .home-delivery__preheader {
    color: var(--text-heading-muted);
    margin-bottom: var(--space-global-xs);
  }

  .home-delivery__title {
    font-family: var(--font-tailor-heading-stack);
    font-size: var(--text-display-size-h2);
    font-weight: var(--heading-weight-h2);
    line-height: var(--lh-heading);
    color: var(--text-heading);
    margin: 0 0 var(--space-global-md);
  }

  .home-delivery__desc {
    font-size: var(--text-prose-size-body);
    color: var(--text-body-muted);
    line-height: var(--lh-body);
    margin: 0 0 var(--space-global-lg);
  }


  .home-delivery__step-text {
    font-size: var(--text-card-size-body);
    color: var(--text-body-muted);
  }

  /* --- §4 Training & support --- */
  .home-training {
    background: var(--bg-page);
    padding: var(--space-struct-y-base) var(--space-global-gutter);
  }

  .home-training__inner {
    max-width: var(--container-max-shell);
    margin: 0 auto;
    width: 100%;
  }

  .home-training__text {
    max-width: var(--container-max-prose);
  }

  .home-training__preheader {
    color: var(--text-heading-muted);
    margin-bottom: var(--space-global-xs);
  }

  .home-training__title {
    font-family: var(--font-tailor-heading-stack);
    font-size: var(--text-display-size-h2);
    font-weight: var(--heading-weight-h2);
    line-height: var(--lh-heading);
    color: var(--text-heading);
    margin: 0 0 var(--space-global-md);
  }

  .home-training__intro {
    font-size: var(--text-prose-size-body);
    color: var(--text-body-muted);
    line-height: var(--lh-body);
    margin: 0 0 var(--space-global-md);
  }

  .home-training__detail {
    font-size: var(--text-card-size-body);
    color: var(--text-body-muted);
    line-height: var(--lh-body);
    margin: 0 0 var(--space-global-lg);
  }

  .home-training__actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-global-sm);
  }

  .home-training__photo {
    box-shadow: var(--shadow-md);
  }

  /* --- §5 Trust signals --- */
  .home-trust {
    padding: var(--space-struct-y-base) var(--space-global-gutter);
  }

  .home-trust__inner {
    max-width: var(--container-max-shell);
    margin: 0 auto;
    width: 100%;
  }

  .home-trust__badges-wrap {
    text-align: center;
    margin-bottom: var(--space-global-xl);
  }

  .home-trust__preheader {
    color: var(--text-heading-muted);
    margin-bottom: var(--space-global-xs);
  }

  .home-trust__title {
    font-family: var(--font-tailor-heading-stack);
    font-size: var(--text-display-size-h3);
    font-weight: var(--heading-weight-h2);
    line-height: var(--lh-heading);
    color: var(--text-heading);
    margin: 0 0 var(--space-global-lg);
  }

  .home-trust__badges {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
    gap: var(--space-global-xl);
  }

  .home-trust__quote-wrap {
    max-width: var(--container-max-prose);
    margin: var(--space-global-xl) auto 0;
    text-align: center;
  }

  .home-trust__quote {
    margin: 0;
    padding: 0;
    border: 0;
  }

  .home-trust__quote-text {
    font-family: var(--font-tailor-body-stack);
    font-size: var(--text-display-size-quote);
    font-weight: var(--font-weight-regular);
    font-style: italic;
    line-height: var(--lh-heading-sub);
    color: var(--text-heading);
    margin: 0 0 var(--space-global-md);
  }

  .home-trust__attribution {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-global-sm);
  }

  .home-trust__cite {
    font-family: var(--font-tailor-body-stack);
    font-size: var(--text-card-size-body);
    font-weight: var(--font-weight-medium);
    color: var(--text-body-muted);
    font-style: normal;
  }

  .home-trust__action {
    margin-top: var(--space-global-lg);
  }

  /* --- §5.5 Selected projects ---
     White ground (matches §2/§4), centred editorial block with a
     monochrome logo strip. Logos are tinted dark via a `brightness(0)`
     filter so mixed partner SVGs read at a uniform visual weight. */
  .home-projects {
    background: var(--bg-page);
    padding: var(--space-struct-y-base) var(--space-global-gutter);
  }

  .home-projects__inner {
    max-width: var(--container-max-reading-wide);
    margin: 0 auto;
    width: 100%;
    text-align: center;
  }

  .home-projects__preheader {
    color: var(--text-heading-muted);
    margin-bottom: var(--space-global-xs);
  }

  .home-projects__title {
    font-family: var(--font-tailor-heading-stack);
    font-size: var(--text-display-size-h2);
    font-weight: var(--heading-weight-h2);
    line-height: var(--lh-heading);
    color: var(--text-heading);
    margin: 0 0 var(--space-global-md);
  }

  .home-projects__body {
    font-size: var(--text-prose-size-body);
    color: var(--text-body-muted);
    line-height: var(--lh-body);
    margin: 0 auto var(--space-global-xl);
    max-width: var(--container-max-prose);
  }

  .home-projects__logos {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-global-xl);
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    gap: var(--space-global-xl);
  }

  .home-projects__logo-item {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .home-projects__logo {
    display: block;
    height: 3rem;
    width: auto;
    max-width: 11rem;
    object-fit: contain;
    filter: grayscale(1) opacity(0.65);
  }

  /* Full-colour override — for logos under mark-protection rules that
     preclude monochrome treatment (e.g. Red Cross emblem). */
  .home-projects__logo--colour {
    filter: none;
  }

  /* Square-logo height bump — compensates for optical under-weighting
     of square marks against wide ones at equal height. Sized so the
     square's bounding-box area roughly matches a wide logo's. */
  .home-projects__logo--square {
    height: 5.5rem;
  }

  .home-projects__action {
    display: flex;
    justify-content: center;
  }

  @media (max-width: 767px) {
    .home-projects__logos {
      gap: var(--space-global-lg);
    }

    .home-projects__logo {
      height: 2.5rem;
    }

    .home-projects__logo--square {
      height: 4.5rem;
    }
  }

  /* --- §6 Book promo ---
     Tailor-layer book signpost. Cover + text + tint primary CTA + text-link
     secondary that signposts to the OtA questions library. */
  .home-book {
    padding: var(--space-struct-y-base) var(--space-global-gutter);
  }

  .home-book__inner {
    max-width: var(--container-max-shell);
    margin: 0 auto;
    width: 100%;
  }

  .home-book__cover-wrap {
    display: flex;
    justify-content: center;
  }

  .home-book__cover {
    display: block;
    width: 12rem;
    height: auto;
    border-radius: var(--radius-sm);
    filter: drop-shadow(var(--book-cover-shadow-1)) drop-shadow(var(--book-cover-shadow-2));
  }

  .home-book__text {
    max-width: var(--container-max-prose);
  }

  .home-book__title {
    font-family: var(--font-tailor-heading-stack);
    font-size: var(--text-display-size-h2);
    font-weight: var(--heading-weight-h2);
    line-height: var(--lh-heading);
    color: var(--text-heading);
    margin: 0 0 var(--space-global-md);
  }

  .home-book__desc {
    font-family: var(--font-tailor-body-stack);
    font-size: var(--text-prose-size-body);
    line-height: var(--lh-body);
    color: var(--text-body-muted);
    margin: 0 0 var(--space-global-lg);
  }

  .home-book__actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-global-md);
  }

  /* --- §7 Topics carousel --- */
  .home-topics {
    background: var(--bg-surface-alt);
    padding: var(--space-struct-y-base) var(--space-global-gutter);
  }

  .home-topics__inner {
    max-width: var(--container-max-shell);
    margin: 0 auto;
    width: 100%;
  }

  .home-topics__header {
    margin-bottom: var(--space-global-lg);
  }

  .home-topics__title {
    font-family: var(--font-tailor-heading-stack);
    font-size: var(--text-display-size-h3);
    font-weight: var(--heading-weight-h2);
    color: var(--text-heading);
    margin: 0 0 var(--space-global-xs);
  }

  .home-topics__intro {
    font-family: var(--font-tailor-body-stack);
    font-size: var(--text-prose-size-body);
    color: var(--text-body-muted);
    margin: 0;
    line-height: var(--lh-body);
  }

  /* Arrow buttons — below carousel, right-aligned */
  .home-topics__nav {
    display: none;
    justify-content: flex-end;
    gap: var(--space-global-xs);
    margin-top: var(--space-global-md);
  }

  @media (min-width: 768px) {
    .home-topics__nav { display: flex; }
  }

  .home-topics__arrow {
    width: 2.5rem;
    height: 2.5rem;
    border-radius: var(--radius-circle);
    border: var(--border-width-xs) solid var(--border-subtle);
    background: var(--bg-page);
    color: var(--text-body);
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: border-color var(--transition-duration) var(--transition-easing),
                color var(--transition-duration) var(--transition-easing);
  }

  .home-topics__arrow:hover:not(:disabled) {
    border-color: var(--brand-accent);
    color: var(--brand-accent-text);
  }

  .home-topics__arrow:disabled {
    opacity: 0.35;
    cursor: default;
  }

  /* Scroll track */
  .home-topics__track {
    display: flex;
    gap: var(--space-global-md);
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
    padding-bottom: var(--space-global-xs);
  }

  .home-topics__track::-webkit-scrollbar { display: none; }

  /* Individual card */
  .home-topics__card {
    flex: 0 0 auto;
    width: 15rem;
    display: flex;
    flex-direction: column;
    gap: var(--space-global-xs);
    padding: var(--space-global-md);
    background: var(--bg-page);
    border: var(--border-width-xs) solid var(--border-subtle);
    border-radius: var(--radius-lg);
    text-decoration: none;
    color: inherit;
    scroll-snap-align: start;
    transition: border-color var(--transition-duration) var(--transition-easing),
                box-shadow var(--transition-duration) var(--transition-easing);
  }

  .home-topics__card:hover {
    border-color: var(--cat-color, var(--brand-accent));
    box-shadow: var(--shadow-md);
  }

  .home-topics__card-illus {
    width: 3.5rem;
    height: 3.5rem;
    background: var(--bg-surface-alt);
    border-radius: var(--radius-md);
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .home-topics__card-illus img {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  .home-topics__card-category {
    font-family: var(--font-tailor-body-stack);
    font-size: var(--text-card-size-h6);
    font-weight: var(--font-weight-semibold);
    text-transform: uppercase;
    letter-spacing: var(--section-eyebrow-ls);
    color: var(--cat-color, var(--text-body-muted));
  }

  .home-topics__card-title {
    font-family: var(--font-tailor-heading-stack);
    font-size: var(--text-card-size-h3);
    font-weight: var(--heading-weight-h3);
    color: var(--text-heading);
    line-height: var(--lh-heading-sub);
  }

  .home-topics__card:hover .home-topics__card-title {
    color: var(--cat-color, var(--brand-accent-text));
  }

  /* "View all" trailing card */
  .home-topics__card--all {
    justify-content: center;
    align-items: center;
    text-align: center;
    background: transparent;
    border-style: dashed;
    min-height: 10rem;
  }

  .home-topics__card--all:hover {
    background: var(--bg-page);
    border-style: solid;
  }

  @media (min-width: 640px) {
    .home-topics__card { width: 16rem; }
  }

  @media (min-width: 1024px) {
    .home-topics__card { width: 18rem; }
  }

  /* --- §8 Closing CTA --- */
  .home-cta {
    background-color: var(--hero-tailor-bg);
    padding: var(--space-global-2xl) var(--space-global-gutter);
  }

  .home-cta__inner {
    max-width: var(--container-max-hero-text);
    margin: 0 auto;
    width: 100%;
    text-align: center;
  }

  .home-cta__title {
    font-family: var(--font-tailor-heading-stack);
    font-size: var(--text-display-size-h2);
    font-weight: var(--heading-weight-h2);
    line-height: var(--lh-heading);
    color: var(--text-heading-on-dark);
    margin: 0 0 var(--space-global-md);
  }

  .home-cta__subtitle {
    font-family: var(--font-tailor-body-stack);
    font-size: var(--text-util-hero-subhead-size);
    font-weight: var(--font-weight-regular);
    line-height: var(--lh-heading-sub);
    color: var(--text-on-dark-strapline);
    margin: 0 auto var(--space-global-lg);
  }

  .home-cta__actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--space-global-sm);
  }

  /* --- Responsive --- */
  @media (min-width: 768px) {
    .hero-teacher__inner {
      grid-template-columns: 1fr 1fr;
      gap: 0;
      align-items: stretch;
    }

    .home-book__cover-wrap {
      justify-content: flex-end;
    }

    .home-book__cover {
      width: 14rem;
    }
  }

  /* Mobile overrides */
  @media (max-width: 767px) {
    .hero-teacher__text {
      padding-top: var(--space-global-2xl);
      padding-bottom: var(--space-global-2xl);
    }

    .hero-teacher__actions .btn,
    .home-training__actions .btn,
    .home-cta__actions .btn {
      width: 100%;
    }

    .home-cta {
      padding: var(--space-global-xl) var(--space-global-gutter);
    }

    .home-trust__badges {
      gap: var(--space-global-lg);
    }
  }
</style>

<script>
  function initTopicsCarousel() {
    const track = document.querySelector<HTMLElement>('.home-topics__track');
    const prevBtn = document.querySelector<HTMLButtonElement>('.home-topics__arrow--prev');
    const nextBtn = document.querySelector<HTMLButtonElement>('.home-topics__arrow--next');
    if (!track || !prevBtn || !nextBtn) return;

    function updateArrows() {
      const { scrollLeft, scrollWidth, clientWidth } = track!;
      prevBtn!.disabled = scrollLeft <= 4;
      nextBtn!.disabled = scrollLeft + clientWidth >= scrollWidth - 4;
    }

    function scroll(direction: number) {
      const card = track!.querySelector<HTMLElement>('.home-topics__card');
      if (!card) return;
      const gap = parseFloat(getComputedStyle(track!).gap) || 16;
      const distance = (card.offsetWidth + gap) * 2;
      track!.scrollBy({ left: direction * distance, behavior: 'smooth' });
    }

    prevBtn.addEventListener('click', () => scroll(-1));
    nextBtn.addEventListener('click', () => scroll(1));
    track.addEventListener('scroll', updateArrows, { passive: true });
    updateArrows();
  }

  document.addEventListener('astro:page-load', initTopicsCarousel);
  document.addEventListener('DOMContentLoaded', initTopicsCarousel);
</script>
